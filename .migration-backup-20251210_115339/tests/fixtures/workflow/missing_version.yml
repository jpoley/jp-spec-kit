description: "Config missing required version field"

states:
  - name: "Specified"

workflows:
  specify:
    command: "/jpspec:specify"
    agents:
      - "product-requirements-manager"
    input_states:
      - "To Do"
    output_state: "Specified"

transitions:
  - from: "To Do"
    to: "Specified"
    via: "specify"
