# VS Code Role Selection - Architecture Diagrams

**Date**: 2025-12-09
**Author**: Platform Engineer Agent

---

## 1. Role Selection Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│  User runs: specify init --here                                      │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│  Constitution Tier Selection                                         │
│  ├─> Light (startups, hobby projects)                                │
│  ├─> Medium (standard business)                                      │
│  └─> Heavy (enterprise, regulated)                                   │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│  NEW: VS Code Role Selection Prompt                                  │
│                                                                       │
│  Select your primary role:                                           │
│    1. Product Manager (PM)     - assess, specify, research           │
│    2. Developer (Dev)           - plan, implement, operate            │
│    3. Security Engineer (Sec)   - security_*, validate (sec)         │
│    4. QA Engineer (QA)          - validate                           │
│    5. Full Workflow (All)       - all commands [default]             │
│                                                                       │
│  Choice [5]: 2                                                       │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│  Multi-Role Support (Optional)                                       │
│                                                                       │
│  Add secondary roles? (y/N): y                                       │
│  Secondary roles (comma-separated): 4,3                              │
│  → Selected: Dev (primary), QA, Sec (secondary)                      │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│  Configuration Scope                                                 │
│                                                                       │
│  Configure role for:                                                 │
│    1. This machine only (user-specific, gitignored)                  │
│    2. Entire team (version-controlled, shared)                       │
│                                                                       │
│  Mode [1]: 2                                                         │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│  Configuration Written                                               │
│  ├─> specflow_workflow.yml (team mode)                                 │
│  │    └─> vscode_roles.default_role: "dev"                           │
│  └─> .vscode/settings.json                                           │
│       ├─> jpspec.vscode.role.primary: "dev"                          │
│       ├─> jpspec.vscode.role.secondary: ["qa", "sec"]                │
│       └─> chat.agent.pinnedAgents: [jpspec-plan, ...]                │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│  Agent Generation (sync-copilot-agents.sh --with-roles)             │
│  ├─> Read role config from specflow_workflow.yml                       │
│  ├─> Add role metadata to .github/agents/*.agent.md                  │
│  │    ├─> roles: ["dev", "all"]                                      │
│  │    └─> priority: 5                                                │
│  └─> Generate 23 agents with role metadata                           │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│  VS Code Copilot Agent Display                                       │
│  ┌────────────────────────────────────────────────────────┐          │
│  │  📌 Pinned Agents (Dev role)                           │          │
│  │    ├─> jpspec-plan (Architecture & Design)            │          │
│  │    ├─> jpspec-implement (Coding & Review)             │          │
│  │    └─> jpspec-operate (Deployment & SRE)              │          │
│  └────────────────────────────────────────────────────────┘          │
│  ┌────────────────────────────────────────────────────────┐          │
│  │  ⭐ Secondary Role Agents (visible, de-prioritized)   │          │
│  │    ├─> jpspec-validate (QA - secondary role)          │          │
│  │    └─> jpspec-security_* (Sec - secondary role)       │          │
│  └────────────────────────────────────────────────────────┘          │
│  ┌────────────────────────────────────────────────────────┐          │
│  │  📋 Utility Agents (always shown)                     │          │
│  │    ├─> speckit-checklist                              │          │
│  │    ├─> speckit-analyze                                │          │
│  │    └─> speckit-constitution                           │          │
│  └────────────────────────────────────────────────────────┘          │
│  ┌────────────────────────────────────────────────────────┐          │
│  │  🔽 Other Agents (collapsed/de-prioritized)           │          │
│  │    └─> jpspec-assess, jpspec-specify (PM role)        │          │
│  └────────────────────────────────────────────────────────┘          │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 2. Configuration Resolution Order

```
┌─────────────────────────────────────────────────────────────────┐
│  User opens VS Code on project                                  │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 1: Read .vscode/settings.json                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  {                                                        │  │
│  │    "jpspec.vscode.role": {                                │  │
│  │      "primary": "dev",                                    │  │
│  │      "secondary": ["qa"],                                 │  │
│  │      "mode": "user"        ← User override               │  │
│  │    }                                                      │  │
│  │  }                                                        │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 2: Read specflow_workflow.yml                              │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  vscode_roles:                                            │  │
│  │    default_role: "all"     ← Team default                │  │
│  │    roles:                                                 │  │
│  │      dev:                                                 │  │
│  │        agents: [jpspec-plan, jpspec-implement, ...]      │  │
│  │      qa:                                                  │  │
│  │        agents: [jpspec-validate, ...]                    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 3: Merge Configuration (Precedence)                      │
│                                                                 │
│  .vscode/settings.json  >  specflow_workflow.yml  >  Defaults    │
│  ──────────────────────    ───────────────────    ────────     │
│  User override              Team default          Hardcoded    │
│  (mode: "user")             (mode: "team")        (mode: N/A)  │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 4: Resolved Configuration                                │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Effective Role Config:                                   │  │
│  │    primary: "dev"          (from .vscode/settings.json)   │  │
│  │    secondary: ["qa"]       (from .vscode/settings.json)   │  │
│  │    mode: "user"            (from .vscode/settings.json)   │  │
│  │                                                           │  │
│  │  Agent List for "dev":                                    │  │
│  │    - jpspec-plan          (from specflow_workflow.yml)      │  │
│  │    - jpspec-implement     (from specflow_workflow.yml)      │  │
│  │    - jpspec-operate       (from specflow_workflow.yml)      │  │
│  │                                                           │  │
│  │  Agent List for "qa":                                     │  │
│  │    - jpspec-validate      (from specflow_workflow.yml)      │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 5: Apply to VS Code Copilot                              │
│  ├─> Pin primary role agents at top of chat                    │
│  ├─> Show secondary role agents with medium priority           │
│  ├─> De-prioritize other role agents (collapsed)               │
│  └─> Always show utility agents (speckit-*)                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. Role-Based Handoff Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  Product Manager Role Workflow                                  │
└─────────────────────────────────────────────────────────────────┘

    ╔══════════════╗
    ║ /jpspec:assess ║  ← PM starts here
    ╚══════════════╝
         │
         │ Assessment Report created
         │
         ▼
    ┌────────────────────────────────────┐
    │  Handoff: ✓ Assessment Complete    │
    │  → Specify Requirements            │
    │  (agent: jpspec-specify)           │
    │  [Click to proceed]                │
    └────────────────────────────────────┘
         │
         ▼
    ╔═══════════════╗
    ║ /jpspec:specify ║
    ╚═══════════════╝
         │
         │ PRD created
         │
         ▼
    ┌────────────────────────────────────┐
    │  Handoff: ✓ Specification Complete │
    │  → Conduct Research                │
    │  (agent: jpspec-research)          │
    │  [Optional - Click to proceed]     │
    └────────────────────────────────────┘
         │                  ┌────────────────────────────────────┐
         │                  │  Handoff: 🔀 Hand off to Developer │
         │                  │  → Plan Architecture               │
         ├─────────────────>│  (agent: jpspec-plan)              │
         │                  │  [Cross-role handoff]              │
         │                  └────────────────────────────────────┘
         ▼                             │
    ╔═══════════════╗                 │
    ║ /jpspec:research ║               │
    ╚═══════════════╝                 │
         │                             │
         │ Research Report             │
         │                             │
         ▼                             │
    ┌────────────────────────────────┐ │
    │  Handoff: 🔀 Hand off to Dev   │ │
    │  → Plan Architecture           │ │
    │  (agent: jpspec-plan)          │ │
    │  [Cross-role handoff]          │ │
    └────────────────────────────────┘ │
         │                             │
         └─────────────────────────────┘
                                       │
                ┌──────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Developer Role Workflow                                        │
└─────────────────────────────────────────────────────────────────┘

    ╔═══════════╗
    ║ /jpspec:plan ║  ← Dev starts here (from PM handoff)
    ╚═══════════╝
         │
         │ ADRs created
         │
         ▼
    ┌────────────────────────────────────┐
    │  Handoff: ✓ Planning Complete      │
    │  → Begin Implementation            │
    │  (agent: jpspec-implement)         │
    │  [Click to proceed]                │
    └────────────────────────────────────┘
         │
         ▼
    ╔═══════════════════╗
    ║ /jpspec:implement ║
    ╚═══════════════════╝
         │
         │ Code & Tests created
         │
         ▼
    ┌────────────────────────────────────┐
    │  Handoff: 🔀 Hand off to QA        │
    │  → Run Validation                  │
    │  (agent: jpspec-validate)          │
    │  [Cross-role handoff]              │
    └────────────────────────────────────┘
         │
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│  QA Engineer Role Workflow                                      │
└─────────────────────────────────────────────────────────────────┘

    ╔═══════════════╗
    ║ /jpspec:validate ║  ← QA starts here (from Dev handoff)
    ╚═══════════════╝
         │
         │ QA + Security Reports
         │
         ▼
    ┌────────────────────────────────────┐
    │  Handoff: 🔀 Hand off to SRE       │
    │  → Deploy to Production            │
    │  (agent: jpspec-operate)           │
    │  [Cross-role handoff]              │
    └────────────────────────────────────┘
         │
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│  SRE/Developer Role Workflow                                    │
└─────────────────────────────────────────────────────────────────┘

    ╔═══════════════╗
    ║ /jpspec:operate ║  ← Dev/SRE deploys
    ╚═══════════════╝
         │
         │ Deployed to Production
         │
         ▼
    ┌────────────────────────────────────┐
    │  Manual: Mark as Done              │
    │  (backlog task edit <id> -s Done)  │
    └────────────────────────────────────┘

Legend:
  ╔═════╗  = /jpspec command (agent invocation)
  ┌─────┐  = Handoff prompt (VS Code UI)
  ✓        = Same-role handoff (smooth transition)
  🔀       = Cross-role handoff (approval gate)
```

---

## 4. Agent Priority & Visibility

```
┌─────────────────────────────────────────────────────────────────┐
│  Agent Visibility Ranking (Developer Role Example)              │
└─────────────────────────────────────────────────────────────────┘

Priority 1: PRIMARY ROLE AGENTS (Pinned at top)
┌────────────────────────────────────────────────┐
│  📌 jpspec-plan         (priority: 1)          │
│  📌 jpspec-implement    (priority: 1)          │
│  📌 jpspec-operate      (priority: 1)          │
└────────────────────────────────────────────────┘
         ↑
         │ Configured in .vscode/settings.json:
         │ "chat.agent.pinnedAgents": ["jpspec-plan", ...]
         │

Priority 2: SECONDARY ROLE AGENTS (Visible, not pinned)
┌────────────────────────────────────────────────┐
│  ⭐ jpspec-validate     (priority: 2)          │
│     (QA - secondary role)                      │
│  ⭐ jpspec-security_*   (priority: 2)          │
│     (Sec - secondary role)                     │
└────────────────────────────────────────────────┘
         ↑
         │ Configured in .vscode/settings.json:
         │ "jpspec.vscode.role.secondary": ["qa", "sec"]
         │

Priority 3: UTILITY AGENTS (Always shown)
┌────────────────────────────────────────────────┐
│  📋 speckit-checklist   (priority: 3)          │
│  📋 speckit-analyze     (priority: 3)          │
│  📋 speckit-tasks       (priority: 3)          │
└────────────────────────────────────────────────┘
         ↑
         │ Always visible regardless of role
         │ (vscode_roles.visibility.show_speckit: true)
         │

Priority 4: OTHER ROLE AGENTS (Collapsed/De-prioritized)
┌────────────────────────────────────────────────┐
│  🔽 jpspec-assess       (priority: 4)          │
│     (PM role - not user's role)                │
│  🔽 jpspec-specify      (priority: 4)          │
│     (PM role - not user's role)                │
│  🔽 jpspec-research     (priority: 4)          │
│     (PM role - not user's role)                │
└────────────────────────────────────────────────┘
         ↑
         │ De-prioritized but still accessible
         │ (vscode_roles.visibility.hide_others: false)
         │

═══════════════════════════════════════════════════
Implementation in Agent Frontmatter:
═══════════════════════════════════════════════════

---
name: "jpspec-plan"
description: "Execute planning workflow..."
target: "chat"
tools: [...]

# NEW: Role metadata
roles:
  - "dev"
  - "all"
priority: 1
---
```

---

## 5. Team vs User Mode

```
┌─────────────────────────────────────────────────────────────────┐
│  TEAM MODE (mode: "team")                                        │
│  Configuration in specflow_workflow.yml (version-controlled)      │
└─────────────────────────────────────────────────────────────────┘

Repository Structure:
  project/
    ├─ specflow_workflow.yml        ← COMMITTED (team config)
    │   └─> vscode_roles:
    │         default_role: "dev"
    │         roles:
    │           dev: {...}
    │           qa: {...}
    │
    ├─ .vscode/
    │   └─ settings.json          ← GITIGNORED (empty or minimal)
    │       {
    │         "chat.promptFiles": true
    │       }
    │
    └─ .gitignore
        └─> .vscode/settings.json

All team members:
  ├─> Clone repository
  ├─> See same default role ("dev")
  ├─> Consistent agent visibility
  └─> No local configuration required

CI/CD Enforcement:
  ├─> Validate specflow_workflow.yml schema
  ├─> Reject PRs with .vscode/settings.json changes
  └─> Ensure role-agent mappings are complete

─────────────────────────────────────────────────────────────────

┌─────────────────────────────────────────────────────────────────┐
│  USER MODE (mode: "user")                                        │
│  Configuration in .vscode/settings.json (gitignored)            │
└─────────────────────────────────────────────────────────────────┘

Repository Structure:
  project/
    ├─ specflow_workflow.yml        ← COMMITTED (fallback config)
    │   └─> vscode_roles:
    │         default_role: "all"  (ignored in user mode)
    │
    ├─ .vscode/
    │   └─ settings.json          ← GITIGNORED (user override)
    │       {
    │         "jpspec.vscode.role": {
    │           "primary": "qa",    ← User's preference
    │           "secondary": ["dev"],
    │           "mode": "user"      ← Enables override
    │         },
    │         "chat.agent.pinnedAgents": ["jpspec-validate"]
    │       }
    │
    └─ .gitignore
        └─> .vscode/settings.json

Individual developers:
  ├─> Configure their own role via /jpspec:reset
  ├─> .vscode/settings.json created locally
  ├─> Not committed to git
  └─> Personal customization

Use Cases:
  ├─> Solo developers with multiple hats
  ├─> Experimentation with different roles
  ├─> Different team members with different roles
  └─> Override team defaults temporarily

─────────────────────────────────────────────────────────────────

Configuration Resolution (Mixed Mode):

  1. Developer A (team mode):
     specflow_workflow.yml → default_role: "dev"
     .vscode/settings.json → (empty)
     ➜ Effective role: "dev"

  2. Developer B (user mode override):
     specflow_workflow.yml → default_role: "dev"
     .vscode/settings.json → primary: "qa", mode: "user"
     ➜ Effective role: "qa" (user override wins)

  3. Developer C (no configuration):
     specflow_workflow.yml → (missing vscode_roles section)
     .vscode/settings.json → (empty)
     ➜ Effective role: "all" (hardcoded default)
```

---

## 6. sync-copilot-agents.sh Enhanced Pipeline

```
┌─────────────────────────────────────────────────────────────────┐
│  INPUT: .claude/commands/{jpspec,speckit}/*.md                  │
│  ├─> jpspec/assess.md                                           │
│  ├─> jpspec/specify.md                                          │
│  ├─> jpspec/plan.md                                             │
│  ├─> ...                                                        │
│  └─> speckit/checklist.md                                       │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 1: Discover & Filter                                      │
│  ├─> Find all *.md files                                        │
│  ├─> Skip partials (_*.md)                                      │
│  └─> Follow symlinks to templates/                              │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 2: Resolve Includes                                       │
│  ├─> Parse {{INCLUDE:path}} directives                          │
│  ├─> Recursively embed content (max depth: 10)                  │
│  ├─> Skip includes inside code blocks                           │
│  └─> Validate no circular references                            │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 3: Extract Frontmatter                                    │
│  ├─> Read YAML frontmatter (description)                        │
│  └─> Remove frontmatter from body                               │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 4: NEW - Get Role Metadata                                │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  get_role_metadata("jpspec", "plan")                    │    │
│  │    ├─> Read specflow_workflow.yml                         │    │
│  │    ├─> Extract vscode_roles.roles.*                     │    │
│  │    ├─> Find matching role for "jpspec-plan"             │    │
│  │    │    └─> roles: ["dev", "all"]                       │    │
│  │    └─> Calculate priority: 1 (high for dev)             │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 5: Generate Enhanced Frontmatter                          │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  ---                                                    │    │
│  │  name: "jpspec-plan"                                    │    │
│  │  description: "Execute planning workflow..."           │    │
│  │  target: "chat"                                         │    │
│  │  tools:                                                 │    │
│  │    - "Read"                                             │    │
│  │    - "Write"                                            │    │
│  │    - ... (existing tools)                              │    │
│  │  roles:               ← NEW                             │    │
│  │    - "dev"                                              │    │
│  │    - "all"                                              │    │
│  │  priority: 1          ← NEW                             │    │
│  │  handoffs:                                              │    │
│  │    - label: "✓ Planning Complete → Begin Implementation"│   │
│  │      agent: "jpspec-implement"                         │    │
│  │      prompt: "..."                                      │    │
│  │      send: false                                        │    │
│  │  ---                                                    │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 6: Combine Frontmatter + Body                             │
│  └─> Create complete .agent.md content                          │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 7: Write Output                                           │
│  ├─> .github/agents/jpspec-plan.agent.md                        │
│  ├─> .github/agents/jpspec-implement.agent.md                   │
│  ├─> ... (23 total agents)                                      │
│  └─> Post-validation (YAML valid, no unresolved includes)       │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  OUTPUT: .github/agents/*.agent.md                              │
│  ├─> 23 agents with role metadata                               │
│  ├─> Priority field for sorting                                 │
│  ├─> Handoffs configured per role                               │
│  └─> Ready for VS Code Copilot consumption                      │
└─────────────────────────────────────────────────────────────────┘

CLI Flags (Enhanced):
  --with-roles        Add role metadata (default: true)
  --role {pm|dev|...} Generate agents for specific role only
  --team-mode         Use specflow_workflow.yml role config
  --user-mode         Use .vscode/settings.json role config
```

---

*Diagrams by Platform Engineer Agent on 2025-12-09*
