name: Docker Publish

on:
  # Trigger on release publication (creates version-tagged images)
  # This is separate from push to avoid paths filter blocking tag triggers
  release:
    types: [published]
  # Trigger on Dockerfile changes to main (updates :latest image)
  push:
    branches: [main]
    paths:
      - '.devcontainer/Dockerfile'
      - '.devcontainer/post-create.sh'
      - '.github/workflows/docker-publish.yml'
  workflow_dispatch:
    inputs:
      force_build:
        description: 'Force rebuild even if no changes'
        required: false
        default: 'false'
        type: boolean

env:
  REGISTRY: docker.io
  IMAGE_NAME: jpoley/flowspec-agents

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      id-token: write  # Required for SLSA provenance attestations

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3.11.1
        with:
          # Use latest buildkit for security fixes including sigstore/timestamp-authority
          version: latest

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_SECRET }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=sha-
            type=ref,event=branch

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .devcontainer
          file: .devcontainer/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          sbom: true  # Generate Software Bill of Materials
          provenance: mode=max  # Generate SLSA provenance attestations

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@0.30.0
        with:
          image-ref: '${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
          exit-code: '1'  # Fail build on HIGH/CRITICAL vulnerabilities
          # Skip attestation layers for vulnerability scan (attestation deps are not runtime)
          skip-files: '.attestation'

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'
        continue-on-error: true

  verify:
    needs: build-and-push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # For attestation verification
    steps:
      - name: Install Cosign
        uses: sigstore/cosign-installer@v3.7.0

      - name: Verify attestations
        env:
          REGISTRY: docker.io
          IMAGE_NAME: jpoley/flowspec-agents
        run: |
          echo "=== Verifying SBOM attestation ==="
          docker buildx imagetools inspect "${REGISTRY}/${IMAGE_NAME}:latest" --format '{{json .SBOM}}' | head -c 500
          echo ""
          echo "=== Verifying Provenance attestation ==="
          docker buildx imagetools inspect "${REGISTRY}/${IMAGE_NAME}:latest" --format '{{json .Provenance}}' | head -c 500
          echo ""
          echo "Attestation verification complete"

      - name: Determine image tag to verify
        id: verify-tag
        run: |
          # For release events, verify the version tag; otherwise verify latest
          if [[ "${{ github.ref }}" =~ ^refs/tags/v ]]; then
            TAG="${{ github.ref_name }}"
            # Remove 'v' prefix for semver tag (v0.2.349 -> 0.2.349)
            TAG="${TAG#v}"
          else
            TAG="latest"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Will verify tag: $TAG"

      - name: Pull and verify image
        env:
          VERIFY_TAG: ${{ steps.verify-tag.outputs.tag }}
        run: |
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERIFY_TAG}
          docker run --rm ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERIFY_TAG} zsh -c "
            echo '=== Verification ===' &&
            node --version &&
            pnpm --version &&
            /home/vscode/.local/bin/uv --version &&
            gh --version | head -1 &&
            echo 'All checks passed!'
          "
